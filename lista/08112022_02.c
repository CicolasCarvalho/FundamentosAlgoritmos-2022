#include <stdio.h>

int main(void) {
    int a[5];

    // int y, *p, x;

    // y = 3;
    // p = &y;
    // x = *p;
    // x = 7;
    // (*p)++; // y = 4
    // x--; // x = 6
    // (*p) = (*p) + x; // (*p) = y = 10

    // printf("y = %i\n", y);

    return 0;
}